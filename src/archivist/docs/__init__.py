# This package exists solely to carry docs/CURSOR_SKILL.md and docs/REFERENCE.md
# as package_data so they are present inside the installed wheel and the Docker
# image (which copies src/ verbatim).  Do not import from this package directly.
