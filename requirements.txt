mcp[http]>=1.9.0,<2.0.0
qdrant-client>=1.13.0,<2.0.0
httpx>=0.28.1
watchfiles>=1.0.0
pyyaml>=6.0.3
tqdm>=4.66.0
uvicorn>=0.46.0
starlette>=0.38.0

# v1.12: async SQLite pool (replaces blocking threading.Lock pattern)
aiosqlite>=0.22.1

# Pydantic Settings v2 (config system — replaces raw os.getenv pattern)
pydantic-settings>=2.7.0

# Dev / test dependencies — see requirements-test.txt (used by CI)
# pytest-benchmark>=5.1.0

# Benchmark evaluation (Tier 3 academic)
# rouge-score>=0.1.2
# nltk>=3.9.0

# Optional: accurate token counting (v1.1 context management)
# Without tiktoken, falls back to chars//4 heuristic
# tiktoken>=0.7.0

# Optional: cross-encoder reranking (RERANKER_ENABLED=true) — see requirements-rerank.txt
# (Not in the default install: pulls PyTorch + large wheels.)

# Optional: OpenTelemetry tracing (set OTEL_EXPORTER_OTLP_ENDPOINT; see observability.py)
# opentelemetry-api>=1.20.0
# opentelemetry-sdk>=1.20.0
# opentelemetry-exporter-otlp>=1.20.0
